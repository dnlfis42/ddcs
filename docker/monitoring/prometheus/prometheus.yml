# 같은 Compose 네트워크의 Controller에서 /metrics를 1초마다 수집한다.

global:
  scrape_interval: 1s
  evaluation_interval: 1s

scrape_configs:
  - job_name: ddcs-controller
    metrics_path: /metrics
    static_configs:
      - targets: ["controller:9000"]
        labels:
          service: ddcs-controller
